using Characters;

namespace Items
{
    public class CrystalScroll : Items
    {
        // Start is called before the first frame update
        protected override void Start()
        {
            base.Start();
        }
        public override void UpdateCharacterStat(Character player)
        {
            base.UpdateCharacterStat(player);
        }
    }
}